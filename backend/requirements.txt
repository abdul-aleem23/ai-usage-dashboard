fastapi>=0.111,<0.116
uvicorn[standard]>=0.30,<0.32
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
httpx>=0.27,<0.29
apscheduler>=3.10,<3.11
beautifulsoup4>=4.12,<4.14
# Playwright is only needed for OPENCODE_MODE=playwright. The import is lazy so
# static-only deployments work without the browser binaries; but the Python
# package must be present when playwright mode is enabled.
playwright>=1.40,<1.50
